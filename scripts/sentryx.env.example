# Copy to /etc/sentryx/sentryx.env (chmod 600, root-owned) and fill in a
# real token. scripts/install.sh generates this automatically — this file
# is only here for people setting things up by hand.
SENTRYX_TOKEN=replace-with-a-long-random-value
